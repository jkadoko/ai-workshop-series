Welcome to the next level of programming!

Out of the box, Python is incredibly capable, but its true power comes from its massive ecosystem of external **packages** (also called libraries).

Think of base Python as a standard, high-quality power unit in a hospital. External packages are the specialized attachments you plug into it—like an ultrasound wand, an ECG monitor, or a surgical drill.

**Pandas** is one of these "attachments." It is the industry standard for data analysis, allowing you to manipulate massive spreadsheets and clinical data sets with just a few lines of code.

To download these attachments, Python uses a built-in tool called **pip** (which stands for "Pip Installs Packages"). Think of pip as your automated supply chain or procurement system.

Here is how to use it:

1. **Open Windows PowerShell:**
**Justification:** Just like before, we need our command-line "operating room" to send instructions to the computer.

**Explanation:** Click your Windows **Start menu**, type PowerShell, and press **Enter**.


2. **Verify Your Supply Chain (pip):**
**Justification:** In standard operating procedures, we always verify our tools are present before starting a procedure. Because you installed Python from the Microsoft Store, pip was automatically installed alongside it.

**Explanation:**

* In PowerShell, type pip --version and press **Enter**.
* You should see a response showing the version number (e.g., pip 24.0). This confirms your procurement tool is ready to go.


3. **Install the Pandas Package:**
**Justification:** We need to command pip to reach out to the official Python repository on the internet, download the pandas tool, and install it into your system.

**Explanation:**

* In PowerShell, type exactly:
pip install pandas
* Press **Enter**.
* You will see several progress bars appear as pip downloads pandas and a few other dependent files it needs to function. Once it returns to a blinking cursor, the installation is complete.


4. **Write a Verification Script:**
**Justification:** We must perform an Installation Qualification (IQ) to prove the software was installed correctly and can be accessed by Python.

**Explanation:**

* Open **Notepad** (Start menu -> type Notepad).
* Type the following two lines exactly as shown:

```python
      

```

python
import pandas as pd
import pandas as pd
print("Pandas successfully imported and ready for data analysis!")
print("Pandas successfully imported and ready for data analysis!")

```
    * *Note: import pandas is the command that tells Python to "plug in the attachment" before running the rest of the script.*
  </Step>

  <Step title="Save the Script">
    **Justification:** We need to save this as a Python file so we can execute it.
    
    **Explanation:** 
    * Click **File** > **Save As**. 
    * Save it to your **Desktop**. 
    * Name the file test_pandas.py. 
    * Crucially, change "Save as type" to **All Files**. 
    * Click **Save**.
  </Step>

  <Step title="Execute the Script">
    **Justification:** Running the script proves that Python can successfully locate and utilize the new pandas package you just downloaded.
    
    **Explanation:** 
    * Go back to your PowerShell window. 
    * Type python  (remember the space at the end!).
    * Drag and drop the test_pandas.py file from your Desktop into the PowerShell window.
    * Press **Enter**.
  </Step>
</Sequence>

```


If you see the message **"Pandas successfully imported and ready for data analysis!"** printed on your screen, congratulations! You have successfully expanded Python's capabilities. You are now fully equipped to start automating complex spreadsheet tasks.